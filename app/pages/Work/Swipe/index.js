import React from 'react';
import {View, Text} from 'react-native';
// import {Swipe, Button} from '../../../components';

const SwipeDemo = () => {
  return (
    <>
      {/* <Swipe
        leftWidth={100}
        rightWidth={100}
        leftAction={
          <Button style={{flex: 1}} shape="square" type="success">
            选择
          </Button>
        }
        rightAction={
          <Button style={{flex: 1}} type="primary" shape="square">
            删除
          </Button>
        }>
        <View>
          <Text>滑动</Text>
        </View>
      </Swipe> */}
    </>
  );
};
export default SwipeDemo;
